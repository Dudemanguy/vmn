project('vmn', 'c')
libconfig = dependency('libconfig')
menu = dependency('menu')
mpv = dependency('mpv')
ncurses = dependency('ncurses')
subdir('include')
inc =  include_directories('include')
vmn_sources = files('main.c', 'config.c')
executable('vmn', vmn_sources, dependencies: [libconfig, menu, mpv, ncurses], include_directories: inc, install: true)
